spring:
  datasource:
    url: jdbc:h2:mem:mato-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  data:
    redis:
      host: localhost
      port: 6379
      password:
  jpa:
    hibernate:
      ddl-auto: update
  jwt:
    secret-key: local-test-secret-key-local-test-secret-key
